{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "palette",
  "description": "Command palette window — hub agent search and voice input",
  "windows": ["command_palette"],
  "permissions": [
    "core:event:default",
    "core:window:allow-start-dragging",
    "palette-window-commands"
  ]
}
